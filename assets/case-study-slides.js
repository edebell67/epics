(()=>{const slides=[...document.querySelectorAll('.slide')],previous=document.querySelector('[data-prev]'),next=document.querySelector('[data-next]'),count=document.querySelector('[data-count]'),bar=document.querySelector('[data-progress]');let index=0;function render(){slides.forEach((slide,i)=>slide.classList.toggle('is-active',i===index));previous.disabled=index===0;next.disabled=index===slides.length-1;count.textContent=`${index+1} / ${slides.length}`;bar.style.width=`${((index+1)/slides.length)*100}%`;history.replaceState(null,'',`#slide-${index+1}`)}function move(delta){index=Math.max(0,Math.min(slides.length-1,index+delta));render()}const hash=Number(location.hash.replace('#slide-',''));if(hash>0&&hash<=slides.length)index=hash-1;previous.addEventListener('click',()=>move(-1));next.addEventListener('click',()=>move(1));document.addEventListener('keydown',event=>{if(event.key==='ArrowLeft')move(-1);if(event.key==='ArrowRight'||event.key===' ') {event.preventDefault();move(1)}});render()})();