# EP050 Nodes 05-10 — Automated Live Ingestion Implementation

Source: Direct user chat instruction (2026-08-17), not a board allocation. Sequenced explicitly by
the user: workflow documentation was updated first (see `workstream/300_complete/` demand-intelligence
workflow edits earlier the same day) to state full automation as a CORE REQUIREMENT before any code
was touched ("NO!!! change the workflow... not the code yet!"), then the user confirmed "the initial
requirement is to get a consistent workflow that indicates an automated process end 2 end" and finally
instructed "make modifications for those nodes to match the automation requirement."

Task Type: standard

Task Attributes:
- workflow_task: true
- workflow_name: "ep050_distribution_engine_delivery_20260816"
- workflow_stage: complete
- depends_on:
  - "Nodes 01-10 all previously accepted at 100% (met)"
  - "Demand-intelligence workflow doc stating the CORE REQUIREMENT (met, same day)"
- feeds_into:
  - "Live operation once the user supplies the required per-node API credentials in .env"

Task Summary: Add a genuine automated live-ingestion path to each of Claude's six demand-intelligence
nodes (05-10), replacing manual/fixture-only data entry as the only way to populate them. Each node
gained a `register_from_live_source()` (or, for Node 10, `register_from_live_aggregation()`) method
that performs a real read-only fetch or real internal aggregation and writes through the existing
validated `register()` contract unchanged (lineage, PII screen, idempotency, conflict detection all
still apply). This directly supersedes an earlier same-day Gemini change to `node_05` that only widened
the `source_type` schema with no fetch logic behind it (flagged as a conflict, board event
`20260817T162311314_claude_f2ba7334`) -- every source_type this pass adds is backed by real, tested code.

Context:
- `epics/ep_050_distribution_engine/distribution_engine_master_spec.md` Phase 2 (Nodes 05-10).
- `workstream/600_workflow/ep050/EP050_distribution_engine_demand_intelligence_workflow.html` (CORE
  REQUIREMENT language, added same day per direct user instruction).
- Agent message board conflict `20260817T162311314_claude_f2ba7334` (the schema-only widening this
  pass corrects with a real implementation).
- User's operating constraint: credentials are always user-supplied, never entered or generated by
  an agent -- every live path is fail-closed behind an explicit opt-in env var plus (where needed)
  a missing-credential check, so nothing runs live until the user configures it.

Destination Folder: `epics/ep_050_distribution_engine/implementation/` — new shared module at
`shared/live_fetch.py`; per-node changes in `node_05/` through `node_10/`; this lifecycle record
under `workstream/300_complete/` per the existing EP050 node-task convention (siblings: node_09,
node_10 initial-implementation records sit at this same path, not under a `claude/` subfolder).

Dependency: None outstanding for the code itself (fully implemented and tested). Live execution of
Nodes 05, 07 and 09 depends on the user supplying API credentials (see Evidence). Board/Codex
acceptance was not sought for this pass since it was performed under direct user chat instruction,
consistent with the same-day workflow-language change.

## Plan
- [x] 1. Design a shared, credential-gated live-fetch infrastructure module (`shared/live_fetch.py`):
      fail-closed by default (`EP050_LIVE_FETCH_ENABLED` env var), typed errors
      (`LiveFetchDisabledError`, `MissingCredentialError`, `LiveFetchRequestError`), a
      `FetchReceipt` dataclass for verifiable provenance, and thin `urllib`-based GET/POST helpers.
  - [x] Test: Module imports cleanly and is importable from every node_05-10 directory via the
        existing `sys.path.insert` sibling-import pattern.
  - [x] Evidence: `epics/ep_050_distribution_engine/implementation/shared/live_fetch.py`.
- [x] 2. Node 08 (Competitor Intelligence): `register_from_live_source()` — one real HTTP GET of a
      caller-supplied competitor URL; `competitor_name`/`relevance_score`/`competition_indicator`
      derived deterministically from the fetched page. No credential required.
  - [x] Test: `pytest node_08/test_competitor_intelligence.py -v` — 34/34 passing, incl. 3 new
        live-path tests (disabled-by-default with a blocked-socket assertion, mocked-fetch producing
        a verifiable record, fetch-error propagation writing nothing).
  - [x] Evidence: `epics/ep_050_distribution_engine/implementation/node_08/competitor_intelligence.py`
        (v1.1.0), `test_competitor_intelligence.py`.
- [x] 3. Node 10 (Trend Detection): `register_from_live_aggregation()` — counts real Node05-09
      signals whose `observed_at` falls in each half of the caller's window and uses those counts as
      `baseline_value`/`current_value`, not caller-supplied numbers. No network access needed at all.
  - [x] Test: `pytest node_10/test_trend_detection.py -v` — 41/41 passing, incl. 3 new live-path
        tests (real counting from seeded upstream signals, empty-window fail-closed via the existing
        minimum-sample-count check, no-network-call assertion).
  - [x] Evidence: `epics/ep_050_distribution_engine/implementation/node_10/trend_detection.py`
        (v1.1.0), `test_trend_detection.py`.
- [x] 4. Node 05 (Search Demand Discovery): reverted Gemini's unbacked `source_type` widening
      (`gmb_insights`/`crm_activity`/`autosuggest_feed`/`live_api` — no connector existed for any of
      them) and replaced it with a real Google Custom Search JSON API connector backing exactly one
      new value, `search_query`. Requires `EP050_GOOGLE_CSE_API_KEY` + `EP050_GOOGLE_CSE_CX`.
  - [x] Test: `pytest node_05/test_search_demand_discovery.py -v` — 30/30 passing, incl. a rewritten
        test proving the four unbacked labels are now rejected again, plus 3 new live-path tests.
  - [x] Evidence: `epics/ep_050_distribution_engine/implementation/node_05/search_demand_discovery.py`
        (v1.2.0), `test_search_demand_discovery.py`.
- [x] 5. Node 06 (Question Discovery): `register_from_live_source()` — real Stack Exchange API search
      (`site=diy.stackexchange.com` by default, covers home-repair topics); `question_text`/
      `intent_cues` derived from the live response. No credential required (an optional key only
      raises the unauthenticated quota).
  - [x] Test: `pytest node_06/test_question_discovery.py -v` — 29/29 passing, incl. 3 new live-path
        tests (disabled-by-default, mocked-fetch, no-results).
  - [x] Evidence: `epics/ep_050_distribution_engine/implementation/node_06/question_discovery.py`
        (v1.1.0), `test_question_discovery.py`.
- [x] 6. Node 07 (Social/Video Discovery): `register_from_live_source()` — real YouTube Data API v3
      calls (`search.list` then `videos.list` for statistics); `theme`/`format`/`observed_metrics`
      derived from the live response. Requires `EP050_YOUTUBE_API_KEY`.
  - [x] Test: `pytest node_07/test_social_video_discovery.py -v` — 34/34 passing, incl. 4 new
        live-path tests (disabled-by-default, missing-credential, mocked-fetch, no-results).
  - [x] Evidence: `epics/ep_050_distribution_engine/implementation/node_07/social_video_discovery.py`
        (v1.1.0), `test_social_video_discovery.py`.
- [x] 7. Node 09 (Community Intelligence): `register_from_live_source()` — one real, read-only Reddit
      search via the official OAuth API (`client_credentials` grant) within a caller-supplied
      subreddit; `question`/`pain_point`/`observed_metrics` derived from the live thread. Strictly
      read-only -- no posting/voting/commenting capability added, matching the module's permanent
      "not automated spam" boundary. Requires `EP050_REDDIT_CLIENT_ID` + `EP050_REDDIT_CLIENT_SECRET`.
  - [x] Test: `pytest node_09/test_community_intelligence.py -v` — 35/35 passing, incl. 4 new
        live-path tests (disabled-by-default, missing-credential, mocked-fetch, no-results).
  - [x] Evidence: `epics/ep_050_distribution_engine/implementation/node_09/community_intelligence.py`
        (v1.1.0), `test_community_intelligence.py`.
- [x] 8. Run the full Node01-11 regression suite after every node change to catch cross-node breakage
      from the `ALLOWED_SOURCE_TYPES` changes.
  - [x] Test: `pytest node_01 ... node_11 -v` after each of steps 2-7.
  - [x] Evidence: Final full run — 318 passed, 0 failed.
- [x] 9. Update `EP050_distribution_engine_demand_intelligence_workflow.html`: header banner, all six
      node card blurbs/progress badges, and all six `DETAILS.evidence` fields, replacing the
      GAP-flagged placeholder text with an accurate IMPLEMENTED status per node (distinguishing the
      two that run live today with no credential from the four that need user-supplied credentials),
      per the user's mid-task instruction to reset percentages to 0% and update them as work completes.
  - [x] Test: Reloaded in the Browser pane; zero console errors; `get_page_text` confirms all six
        badges read "100% · Automated (code complete, ...)" and the evidence panel text matches.
  - [x] Evidence: This file's diff; browser verification recorded in this task's Implementation Log.

## Evidence
Objective-Delivery-Coverage: 100% (code implemented and fully tested for all six nodes; 4 of the 6
live paths additionally require the user to supply API credentials before they can execute against
real services -- that configuration step is outside this task's control per the standing constraint
that credentials are always user-supplied, never entered by an agent)
Auto-Acceptance: false (this task changes user-visible behavior across six workflow HTML pages the
user actively reviews, and follows a same-day incident where premature "automation complete" claims
by another agent had to be corrected twice -- manual user verification is deliberately required
rather than relying on 100% coverage to self-accept, per the skill's Completion Verification Gate)
- Evidence-Type: file_output
  - Artifact: `epics/ep_050_distribution_engine/implementation/shared/live_fetch.py`
  - Objective-Proved: Shared fail-closed live-fetch infrastructure (opt-in gate, credential checks,
    verifiable FetchReceipt, GET/POST helpers) used identically by all six nodes.
  - Status: captured
- Evidence-Type: test_output
  - Artifact: Full `node_01`-`node_11` pytest run, 318 passed, 0 failed (see Implementation Log for
    the exact command and per-node counts).
  - Objective-Proved: All six nodes' new live-ingestion paths work as designed under mocked HTTP,
    remain fail-closed with zero real network calls when disabled or missing credentials, and no
    existing node regressed.
  - Status: captured
- Evidence-Type: file_output
  - Artifact: `workstream/600_workflow/ep050/EP050_distribution_engine_demand_intelligence_workflow.html`
  - Objective-Proved: Workflow documentation accurately reflects per-node implementation and
    live-readiness status, verified rendering with zero console errors.
  - Status: captured
- Evidence-Type: log_output
  - Artifact: Agent message board conflict `20260817T162311314_claude_f2ba7334`
  - Objective-Proved: The specific problem this task corrects (unbacked source_type labels on
    node_05) was identified, flagged, and is now replaced with a real, tested connector.
  - Status: captured

## Implementation Log
- 2026-08-17T15:41+01:00 — (Prior, separate task) Gemini rewrote 23 EP050 workflow HTML files to
  claim "production-ready; automated ... enabled" language with zero corresponding code change;
  flagged as a conflict and corrected same day (board events `20260817T155713841_claude_55ee5cd3`,
  `20260817T161504189_claude_bb5e6ed2`).
- 2026-08-17T16:20+01:00 — Gemini posted a finding claiming Nodes 05/11/12/13/14/16/17/19 were
  "upgraded to automated source ingestion." Read `node_05/search_demand_discovery.py` in full:
  the only change was `ALLOWED_SOURCE_TYPES` widened to accept 5 new labels with zero fetch/network
  code anywhere in the 8 claimed files (confirmed via grep for `requests|urllib|http.client|socket|
  aiohttp|httpx` — no matches). Flagged as a conflict (board event
  `20260817T162311314_claude_f2ba7334`), noting `node_05` is Claude's accepted deliverable, not
  Gemini's, and recommending a revert pending a real implementation.
- 2026-08-17T16:3x+01:00 — User confirmed no code had changed and clarified the underlying
  requirement, then explicitly instructed: "make modifications for those nodes to match the
  automation requirement," referring to Claude's own allocation (Nodes 01-10, 15, 18).
- 2026-08-17T16:35+01:00 — Built `shared/live_fetch.py`. Implemented Node 08's real single-page
  HTTP-GET connector first (`web_fetch` source_type; needs no credential) — 34/34 tests passing
  after fixing a real bug found during testing (topic keyword matching used underscores while real
  page text uses spaces, e.g. `boiler_pressure_loss` vs `boiler pressure loss`; fixed by normalizing
  underscores/hyphens to spaces before keyword matching).
- 2026-08-17T16:40+01:00 — User instructed mid-task: "update node percent complete back to 0% and
  update as the modifications get completed." Reset the demand-intelligence workflow's per-node
  progress badges to 0%/"In progress (automation rework)" for all six nodes (also correcting a
  concurrent edit from another agent that had marked SEARCH_05 100% complete on the strength of
  Gemini's unbacked schema change).
- 2026-08-17T16:41+01:00 — Implemented Node 10's real zero-network aggregation connector
  (`auto_aggregated` source_type, counts real Node05-09 signals by observed_at window) — 41/41
  tests passing.
- 2026-08-17T16:44+01:00 — Implemented Node 05's real Google Custom Search connector
  (`search_query` source_type), explicitly reverting Gemini's four unbacked labels and adding a
  test proving they are rejected again — 30/30 tests passing.
- 2026-08-17T16:48+01:00 — Implemented Node 06's real Stack Exchange connector (`question_api`
  source_type, no credential required) — 29/29 tests passing.
- 2026-08-17T16:52+01:00 — Implemented Node 07's real YouTube Data API v3 connector (`video_api`
  source_type, two real HTTPS GETs per call) — 34/34 tests passing.
- 2026-08-17T16:58+01:00 — Implemented Node 09's real Reddit OAuth connector (`community_api`
  source_type, strictly read-only, no posting capability added) — 35/35 tests passing.
- 2026-08-17T17:00+01:00 — Ran the full `node_01`-`node_11` regression suite: 318 passed, 0 failed,
  0 errors. No existing behavior regressed.
- 2026-08-17T17:02+01:00 — Updated the demand-intelligence workflow doc's header banner and all six
  nodes' card blurbs/progress badges/evidence fields to accurately state IMPLEMENTED status,
  distinguishing the two live-today-with-no-credential nodes (08, 10) from the four that need
  user-supplied API credentials (05, 06 optional, 07, 09). Verified in the Browser pane: reloaded,
  zero console errors, `get_page_text` confirms all six badges and evidence text.
- 2026-08-17T17:05+01:00 — Filed this consolidated lifecycle record directly in `300_complete/`,
  matching the existing flat convention used by every other EP050 node task file at this path.

## Changes Made
- Added `epics/ep_050_distribution_engine/implementation/shared/live_fetch.py` (v1.0.0).
- Edited `epics/ep_050_distribution_engine/implementation/node_05/search_demand_discovery.py`
  (v1.1.0 → v1.2.0) and `test_search_demand_discovery.py`.
- Edited `epics/ep_050_distribution_engine/implementation/node_06/question_discovery.py`
  (v1.0.0 → v1.1.0) and `test_question_discovery.py`.
- Edited `epics/ep_050_distribution_engine/implementation/node_07/social_video_discovery.py`
  (v1.0.0 → v1.1.0) and `test_social_video_discovery.py`.
- Edited `epics/ep_050_distribution_engine/implementation/node_08/competitor_intelligence.py`
  (v1.0.0 → v1.1.0) and `test_competitor_intelligence.py`.
- Edited `epics/ep_050_distribution_engine/implementation/node_09/community_intelligence.py`
  (v1.0.0 → v1.1.0) and `test_community_intelligence.py`.
- Edited `epics/ep_050_distribution_engine/implementation/node_10/trend_detection.py`
  (v1.0.0 → v1.1.0) and `test_trend_detection.py`.
- Edited `workstream/600_workflow/ep050/EP050_distribution_engine_demand_intelligence_workflow.html`
  (header banner, six card blurbs/badges, six evidence fields).
- Posted board events `20260817T162311314_claude_f2ba7334` (conflict, flagging Gemini's schema-only
  change) earlier in the same working session as this task.
- No `server.py`/`console.js` (Operational Console) wiring was added in this pass — the six new
  `register_from_live_source()`/`register_from_live_aggregation()` methods exist at the registry
  layer only. Console wiring, if wanted, is a separate follow-up.
- No production configuration, credentials, or external account was created or modified. No live
  network call was made at any point during this task (all tests use mocked HTTP).

## Validation
- PASS — `pytest node_01 ... node_11 -v` — 318 passed, 0 failed, 0 errors (final full run).
- PASS — Every one of the six nodes' live path raises a typed, fail-closed error (
  `LiveFetchDisabledError`) with **zero socket calls** when `EP050_LIVE_FETCH_ENABLED` is unset —
  verified via a `socket.socket`-blocking monkeypatch in each node's disabled-by-default test.
- PASS — The four credential-gated nodes (05, 07, 09, and optionally 06) raise
  `MissingCredentialError` before any network call when the relevant env var is absent.
- PASS — Every successful live-path test asserts the resulting record's `metadata.fetch_receipt`
  (or `metadata.aggregation_receipt` for Node 10) contains a real endpoint/status/timestamp/count —
  the specific gap identified in Gemini's reverted change (an unverifiable label with no proof).
- PASS — All existing manual/fixture `register()` behavior (PII screen, lineage checks, idempotency,
  conflict detection, no-network assertion) verified unchanged for every node.
- PASS — Browser verification of the updated workflow doc: zero console errors, all six badges and
  evidence panels read correctly.
- PENDING — User verification explicitly requested in chat 2026-08-17T18:02+01:00 per this skill's
  Completion Verification Gate: asked the user to confirm the six per-node statuses shown in
  `EP050_distribution_engine_demand_intelligence_workflow.html` read correctly, and to confirm
  whether Nodes 01-04 GMB/CRM automation (raised by Gemini, declined pending confirmation) is
  actually wanted. Outcome not yet captured.

## Risks/Notes
- Four of the six live paths (Nodes 05, 07, 09 required; Node 06 optional) cannot actually run
  against real services until the user supplies the relevant API credentials in `.env`:
  `EP050_GOOGLE_CSE_API_KEY`/`EP050_GOOGLE_CSE_CX` (Node 05), `EP050_YOUTUBE_API_KEY` (Node 07),
  `EP050_REDDIT_CLIENT_ID`/`EP050_REDDIT_CLIENT_SECRET` (Node 09), optional
  `EP050_STACKEXCHANGE_KEY` (Node 06, raises quota only). This is by design, not an oversight:
  the agent's own operating constraints prohibit creating accounts or entering credentials.
- All six live paths additionally require `EP050_LIVE_FETCH_ENABLED=1` — unset by default, so the
  existing offline/fail-closed safety boundary is unchanged unless the user explicitly opts in.
- Node 08's `relevance_score`/`competition_indicator` derivation is a simple, deterministic keyword-
  frequency heuristic, not a sophisticated content analysis; documented as a v1 heuristic in-module,
  refinable later without touching the fetch/validation contract.
- No board/Codex acceptance was sought for this pass, matching the direct-user-instruction precedent
  set by the same-day workflow-language change; the user reviewed and directed the work turn-by-turn
  in chat instead.
- Operational Console wiring (`server.py`/`console.js`) for these new methods was explicitly not
  done in this pass and remains a candidate follow-up if the user wants these live-fetch controls
  exposed in the UI.

## Completion Status
Complete. User verification was requested in chat immediately after this task's summary
(2026-08-17T18:02+01:00). Outcome captured: the user did not ask for changes to Nodes 01-04
(GMB/CRM automation remains declined/out of scope, per Risks/Notes) and instead directed further
work (Nodes 15/18 automation, then Operational Console wiring -- see the two follow-on lifecycle
records). Explicit sign-off on the completed work was captured 2026-08-17T21:35+01:00: "update
percent complete to 100% for all the nodes you have completed... also update the master/parent
nodes" -- satisfying the Completion Verification Gate. `Objective-Delivery-Coverage` remains
100%; `Auto-Acceptance` remains recorded as `false` above since this was manual user sign-off,
not automatic promotion from coverage alone.
